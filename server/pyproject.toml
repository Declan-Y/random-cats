[project]
name = "server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "boto3[crt]>=1.43.79",
    "dotenv>=0.9.9",
    "fastapi[standard]>=0.141.1",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
